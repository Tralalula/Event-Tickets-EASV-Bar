package event.tickets.easv.bar.gui.common;

/**
 * Each WindowType is a distinct context or functional area, such as authentication or the main application interface,
 * which allows for the organization and association of ViewType instances.
 */
public enum WindowType {
    None,
    MAIN_APP,
    AUTH
}
